import { NumberField } from "@base-ui-components/react/number-field";

const NumberInput = ({ ...props }: NumberField.Input.Props) => {
  return <NumberField.Input {...props} />;
};

export default NumberInput;
